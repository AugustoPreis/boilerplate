# Prettier's built-in Handlebars parser (glimmer) doesn't support partial
# blocks ({{#> base}}...{{/base}}), which core/mail/templates/*.hbs relies
# on (see apps/api/docs/mailing.md). Format these by hand instead.
*.hbs

# Generated by TanStack Router on every `dev`/`build` run, without a
# trailing-semicolon pass — formatting it here just fights that regeneration.
apps/web/src/app/routeTree.gen.ts
